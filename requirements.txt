# Store Intelligence API — Python dependencies
# Pin major versions for reproducibility; minor updates allowed

# Web framework
fastapi==0.111.*
uvicorn[standard]==0.30.*
pydantic==2.7.*
httpx==0.27.*

# Database
aiosqlite==0.20.*

# Pipeline (detection) — installed separately in the detection container
# Listed here for completeness / local dev
ultralytics>=8.2.0        # YOLOv8
opencv-python-headless>=4.9.0
numpy>=1.26.0
requests>=2.31.0

# Dashboard
rich>=13.7.0

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
